import 'package:flutter/material.dart';

class VehicleDetail extends StatelessWidget {
  static const routeName = '/vehicleDetail';

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}
